name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # UI & Icons
  cupertino_icons: ^1.0.8
  firebase_core: ^2.24.0
  firebase_auth: ^4.17.0
  cloud_firestore: ^4.9.0
  get: ^4.7.3
  pinput: ^6.0.1
  shared_preferences: ^2.2.2
  lottie: ^3.0.0
  animated_text_kit: ^4.2.2
  supabase_flutter: ^2.12.0
  lottie: ^3.0.0
  animated_text_kit: ^4.2.2
  pinput: ^6.0.1

  # State management
  get: ^4.6.6

  # Storage
  shared_preferences: ^2.2.2

  # Firebase (UPDATED – REQUIRED)
  firebase_core: ^3.15.0
  firebase_auth: ^5.1.0

  # Location (UPDATED – COMPATIBLE)
  geolocator: ^13.0.1
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/lottie/
    - assets/images/
