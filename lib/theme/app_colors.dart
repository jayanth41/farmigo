import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF3FA34D); // Green button, active icons
  static const primaryDark = Color(0xFF2F8F3A);
  static const bgSoft = Color(0xFFF2FBF4); // Light green bg
  static const white = Color(0xFFFFFFFF);

  static const border = Color(0xFFD1D5DB); // Input border
  static const textMain = Color(0xFF374151); // Headings
  static const textMuted = Color(0xFF9CA3AF); // Placeholder
  static const iconGrey = Color(0xFF6B7280);
}
