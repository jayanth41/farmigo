// HomePage removed — this file was an experimental prototype and is intentionally
// left as an empty placeholder to avoid accidental references. The app now
// uses `HomeScreen` (lib/screens/home_screen.dart) for the main '/home' route.

